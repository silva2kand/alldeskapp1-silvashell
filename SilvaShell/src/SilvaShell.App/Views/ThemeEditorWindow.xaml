<Window x:Class="SilvaShell.App.Views.ThemeEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Theme Editor" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Customize Theme Colors"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>

                <!-- Primary Color -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Primary Color:" VerticalAlignment="Center"/>
                <TextBox x:Name="PrimaryColorTextBox" Grid.Row="0" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="0" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="PrimaryColorPreview" Width="40" Height="20" Fill="Blue"/>
                </Border>

                <!-- Secondary Color -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Secondary Color:" VerticalAlignment="Center"/>
                <TextBox x:Name="SecondaryColorTextBox" Grid.Row="1" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="1" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="SecondaryColorPreview" Width="40" Height="20" Fill="LightBlue"/>
                </Border>

                <!-- Background Color -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Background Color:" VerticalAlignment="Center"/>
                <TextBox x:Name="BackgroundColorTextBox" Grid.Row="2" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="2" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="BackgroundColorPreview" Width="40" Height="20" Fill="White"/>
                </Border>

                <!-- Foreground Color -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Foreground Color:" VerticalAlignment="Center"/>
                <TextBox x:Name="ForegroundColorTextBox" Grid.Row="3" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="3" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="ForegroundColorPreview" Width="40" Height="20" Fill="Black"/>
                </Border>

                <!-- Accent Color -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Accent Color:" VerticalAlignment="Center"/>
                <TextBox x:Name="AccentColorTextBox" Grid.Row="4" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="4" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="AccentColorPreview" Width="40" Height="20" Fill="Orange"/>
                </Border>

                <!-- Sidebar Background -->
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Sidebar Background:" VerticalAlignment="Center"/>
                <TextBox x:Name="SidebarBackgroundTextBox" Grid.Row="5" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="5" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="SidebarBackgroundPreview" Width="40" Height="20" Fill="#111111"/>
                </Border>

                <!-- Sidebar Foreground -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Sidebar Foreground:" VerticalAlignment="Center"/>
                <TextBox x:Name="SidebarForegroundTextBox" Grid.Row="6" Grid.Column="1" Margin="0,4"/>
                <Border Grid.Row="6" Grid.Column="2" Margin="4" BorderThickness="1" BorderBrush="Black">
                    <Rectangle x:Name="SidebarForegroundPreview" Width="40" Height="20" Fill="White"/>
                </Border>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Preview" Width="80" Margin="0,0,8,0" Click="PreviewButton_Click"/>
            <Button Content="Save" Width="80" Margin="0,0,8,0" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
