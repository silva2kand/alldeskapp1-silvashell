<Window x:Class="SilvaShell.App.Views.AddAppWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Custom App" Height="320" Width="450"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="App Name:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="NameBox" Height="25" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="URL:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="UrlBox" Height="25" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Icon Path (optional):" Margin="0,0,0,5"/>
        <DockPanel Grid.Row="5" Margin="0,0,0,10">
            <Button DockPanel.Dock="Right" Content="Browse" Width="60" Margin="5,0,0,0" Click="BrowseIcon_Click"/>
            <TextBox x:Name="IconBox" Height="25"/>
        </DockPanel>

        <CheckBox Grid.Row="6" x:Name="TerminalCheck" Content="Allow Terminal Access" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Add" Width="80" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
